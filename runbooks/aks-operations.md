# AKS Operations
- Review node pool autoscaler events and adjust `node_count` bounds per environment.
- Rotate ACR/AKS credentials with GitHub OIDC annually.
- Validate CSI Secret Store sync with Key Vault following key rotations.
- Keep ingress-nginx Helm chart values aligned with security updates.
